# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# supabase
/supabase/.branches
/supabase/.temp
.env*
!.env.example

# Private operator state and generated evidence are never public source.
/work/
/outputs/
/automation-state/
/automation-receipts/
**/__pycache__/
*.pyc
/.pytest_cache/
/operations/reference/.pytest_cache/
/operations/reference/*-state.json
/operations/reference/*-state.json.*
/operations/reference/*.lock.json
/operations/reference/mmtt-event-cadence-batch-*.json
/operations/reference/automation-control.json
/operations/reference/outputs/
/operations/reference/work/
/operations/reference/.env*

# Local foundation receipts may include production environment exports. They are
# operational evidence, never deployable or committable source.
/.foundation-run/

# Background art ships IN the repo — Vercel's GitHub integration auto-deploys from
# the git tree, so anything outside git silently vanishes from production builds.
tsconfig.tsbuildinfo
